\* TLC configuration: quorum enabled, no partitions.
\*
\* Setup:   3 peers, quorum required (majority = 2), partitions disabled.
\* Expect:  MutualExclusion HOLDS. TLC explores the full state space and
\*          finds no counter-example.
\*
\* Run:
\*    java -jar tla2tools.jar -config LockSpec_Safe.cfg LockSpec.tla

SPECIFICATION Spec

CONSTANTS
    Peers            = {p1, p2, p3}
    MaxAttempts      = 2
    RequireQuorum    = TRUE
    EnableDisconnect = FALSE

INVARIANTS
    TypeInv
    MutualExclusion


CHECK_DEADLOCK FALSE
