\* TLC configuration for the FIFO variant: 3 peers, quorum, no partitions.
\* Expected: MutualExclusion holds. Demonstrates that per-(sender,receiver)
\* FIFO ordering + requestID-tagged Release messages fix the reorder bug
\* found in LockSpec_Safe.cfg.

SPECIFICATION Spec

CONSTANTS
    Peers            = {p1, p2, p3}
    MaxAttempts      = 2
    RequireQuorum    = TRUE
    EnableDisconnect = FALSE

INVARIANTS
    TypeInv
    MutualExclusion

CHECK_DEADLOCK FALSE
