\* Add statements after this line.
\* Same setup as LockSpecFIFO_PartialVisibility.cfg, but against the
\* fixed spec which models the LockAcquireMembershipWait warm-start
\* guard. Expected: MutualExclusion holds.

SPECIFICATION Spec

CONSTANTS
    Peers            = {p1, p2, p3}
    MaxAttempts      = 2
    RequireQuorum    = TRUE
    EnableDisconnect = FALSE
    PreHeldPeer      = p1

INVARIANTS
    TypeInv
    MutualExclusion

CHECK_DEADLOCK FALSE
