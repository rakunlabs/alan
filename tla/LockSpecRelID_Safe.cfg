\* Question: is requestID-tagged Release ALONE (without FIFO) enough?
\* Setup mirrors LockSpec_Safe.cfg.

SPECIFICATION Spec

CONSTANTS
    Peers            = {p1, p2, p3}
    MaxAttempts      = 2
    RequireQuorum    = TRUE
    EnableDisconnect = FALSE

INVARIANTS
    TypeInv
    MutualExclusion

CHECK_DEADLOCK FALSE
